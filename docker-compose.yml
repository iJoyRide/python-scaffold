services:
  backend:
    build: 
      context: .
      dockerfile: Dockerfile.backend
    env_file:
      - backend/.env
    ports:
      - "8081:8081"
    networks:
      - cybereye-network 

networks:
  cybereye-network:
    driver: bridge







  